# hello :)) i guess you came here to see the tutorial. so yea look down bro.

first: you would more likely need to **download the file and then un-compress** it.

second: rename **config.example.py** to >> **config.py**

third: go to https://serper.dev/ and **get an api key (DONT SHARE IT WITH ANYONE)**

fourth: **paste** the **api key in config.py**

fifth: run **python3 app.py** and then open localhost:5000 . thats all, thanks
